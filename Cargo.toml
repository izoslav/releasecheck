[package]
name = "releasecheck"
version = "0.1.0"
edition = "2018"

[dependencies]
clap = "3.0.0-beta.4"
reqwest = { version = "0.11.4", features = ["blocking", "json"] }
serde = { version = "1.0.130", features = ["derive"] }
